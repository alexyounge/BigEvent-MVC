@if(Request.IsAuthenticated) {

    @:@Html.ActionLink("My Account" ,"OrderDetails", "Order", new { username = User.Identity.Name}, "")
}
else {
    @:@Html.ActionLink("My Account (Log in)", "Logon", "Account")
}
